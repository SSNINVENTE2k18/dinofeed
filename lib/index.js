'use strict';

var eventlist = require('../data/eventlist.json');
var department = require('../data/dept.json');
var eventdetail = require('../data/eventdetail');

module.exports = {
	eventlist: eventlist, department: department, eventdetail: eventdetail
};
